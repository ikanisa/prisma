# ==== App basics ====
NODE_ENV=production
PORT=3000
NEXT_TELEMETRY_DISABLED=1
API_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Public admin hostname (docs/scripts use this)
ADMIN_HOSTNAME=admin.sacco-plus.com

# ==== Supabase (Public - safe for client) ====
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=PASTE_ANON_KEY_HERE

# ==== Supabase (SERVER-ONLY - Never expose to client) ====
# SUPABASE_URL=https://YOUR_PROJECT.supabase.co                    # SERVER-ONLY
# SUPABASE_SERVICE_ROLE_KEY=PASTE_SERVICE_ROLE_KEY_HERE           # SERVER-ONLY
# SUPABASE_JWT_SECRET=your-jwt-secret                              # SERVER-ONLY

# ==== Database (SERVER-ONLY) ====
# DATABASE_URL=postgresql://user:pass@host:5432/dbname            # SERVER-ONLY
# DIRECT_URL=postgresql://user:pass@host:5432/dbname?pgbouncer=true # SERVER-ONLY

# ==== External APIs (SERVER-ONLY) ====
# OPENAI_API_KEY=sk-...                                            # SERVER-ONLY
# SMTP_PASSWORD=smtp-password                                      # SERVER-ONLY
# STRIPE_SECRET_KEY=sk_...                                         # SERVER-ONLY

# ==== Encryption & Signing Keys (SERVER-ONLY) ====
# KMS_DATA_KEY=base64-encoded-key                                  # SERVER-ONLY
# KMS_DATA_KEY_BASE64=base64-encoded-key                           # SERVER-ONLY
# REPORT_SIGNING_KEY=base64-signing-key                            # SERVER-ONLY
# HMAC_SHARED_SECRET=hmac-secret                                   # SERVER-ONLY
# TRUSTED_COOKIE_SECRET=cookie-secret                              # SERVER-ONLY
# BACKUP_PEPPER=pepper-value                                       # SERVER-ONLY

# ==== Managed encryption (CMEK/KMS) ====
# SUPABASE_ENCRYPTION_PROVIDER=aws
# SUPABASE_ENCRYPTION_KEY_REFERENCE=arn:aws:kms:us-east-1:123456789012:key/your-supabase-key
# OBJECT_STORAGE_ENCRYPTION_PROVIDER=aws
# OBJECT_STORAGE_ENCRYPTION_KEY_REFERENCE=arn:aws:kms:us-east-1:123456789012:key/your-storage-key
# JOB_QUEUE_ENCRYPTION_PROVIDER=aws
# JOB_QUEUE_ENCRYPTION_KEY_REFERENCE=arn:aws:kms:us-east-1:123456789012:key/your-queue-key

# ==== (Optional) Next.js absolute URL ====
# NEXT_PUBLIC_SITE_URL=https://admin.sacco-plus.com
