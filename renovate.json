{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base"],
  "enabled": true,
  "packageRules": [
    {
      "matchManagers": ["npm"],
      "enabled": false,
      "description": "Temporarily pause npm updates while the refactor is in progress"
    }
  ]
}
